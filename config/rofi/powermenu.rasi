/* --- Posición --- */
configuration {
  location: 1;
  xoffset: 5;
  yoffset: 50;
}

/* --- Paleta como variables --- */
* {
  bg:        #000000;   /* Fondo */
  fg:        #c5c8c6;   /* Texto */
  sel:       #FF0033;   /* Seleccionado */
  font: "Hack Nerd Font 12";
}

/* --- Ventana --- */
window {
  background-color: @bg;
  border: 0;
  border-radius: 8px;
  padding: 10px;
  width: 10%;
}

/* --- Contenedores --- */
mainbox {
  background-color: @bg;
  padding: 0;
  children: [ listview ];
}
listview {
  background-color: @bg;
  lines: 5;
  fixed-height: true;
  padding: 6px;
  spacing: 6px;
}

/* --- Elementos --- */
element {
  background-color: @bg;
  text-color: @fg;
  padding: 6px 12px;
}
element-text { 
  highlight: bold italic;
  text-color: @fg; 
  background-color: inherit;
}
element selected {
  background-color: @sel;
  text-color: #ffffff;
  border-radius: 6px;
}

/* --- Barra superior de dmenu (prompt + entrada) --- */
inputbar {
  background-color: @bg;
  text-color: @fg;
  padding: 6px 10px;
  border: 0;
}

prompt {
  background-color: @bg;
  text-color: @fg;
  padding: 0 6px 0 0;
}

/* El ":" que rofi añade después del prompt es otro widget */
textbox-prompt-colon {
  background-color: @bg;
  text-color: @fg;
}

entry {
  background-color: @bg;
  text-color: @fg;
  placeholder-color: @fg;
  cursor: text;
}

